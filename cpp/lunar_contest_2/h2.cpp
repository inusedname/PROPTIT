#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    long long n, k;
    cin >> n >> k;
    if (n==1||k==1||k==2)
    {
        cout << "0";
        return 0;
    }
    long long dem=0;
    for (int i = 1; i <= floor(k / 2);i++)
        if (k-)
}